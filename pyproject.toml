[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "enn"                  # pip install enn
version = "0.0.1"
dependencies = ["torch>=2.0"]

[tool.setuptools]
packages = ["enn"]            # ← explicit, no auto-discovery

